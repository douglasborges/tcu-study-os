# Test Report — v11

Testes automatizados executados em Chromium headless com o backup antigo como entrada:

- migração para schema 12;
- 77 sessões antigas preservadas;
- ciclo ativo do backup preservado (AFO, Português e DCON; DAD continuou em espera);
- Licitações renomeada sem perder as 2 sessões históricas;
- TI substituída pela nova trilha TI Total;
- 9 disciplinas Missão Cebraspe criadas;
- DAD Rafael Oliveira criado;
- zero tópicos órfãos após migração;
- zero ocorrências de `Portuguêsaria` após migração;
- tópicos Em espera posicionados antes dos já trabalhados no seletor;
- registro de 1h35min gravado como 95 minutos;
- edição para 2h05min gravada como 125 minutos;
- Revisão Noturna registrada e incluída no total diário;
- interface de Progresso exibindo horas/minutos em vez de decimal;
- sintaxe JavaScript validada com `node --check`.
